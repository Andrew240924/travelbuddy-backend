1. Auth

Регистрация и вход пользователя.

API

POST /auth/register (сделано)
POST /auth/login (сделано)
GET /auth/profile (не сделано)


2. Users

Работа с профилем пользователя.

API

GET /users/:id (не сделано)
PATCH /users/:id (не сделано)
GET /users/:id/routes (не сделано)
GET /users/me (сделано)


3. Routes (основная часть сайта) (сделано)

Создание и просмотр маршрутов.

API

POST /routes (сделано)
GET /routes (сделано)
GET /routes/:id (сделано)
PATCH /routes/:id (сделано)
DELETE /routes/:id (сделано)
GET /routes/public (сделано, есть в коде)

Фильтры:

GET /routes?category= (не сделано)
GET /routes?author= (не сделано)
GET /routes?public=true (не сделано, вместо этого есть GET /routes/public)


4. Route Points

Точки маршрута.

POST /routes/:id/points (сделано)
GET /routes/:id/points (сделано)
PATCH /points/:id (сделано)
DELETE /points/:id (сделано)


5. Categories (сделано)

Категории маршрутов.

GET /categories (сделано)
POST /categories (сделано)
POST /routes/:id/categories (сделано)
DELETE /routes/:id/categories/:categoryId (сделано)


6. Favorites

Избранное.

POST /favorites/:routeId (сделано)
DELETE /favorites/:routeId (сделано)
GET /favorites (сделано)


7. Saved Routes

Сохранённые маршруты.

POST /saved/:routeId (сделано)
DELETE /saved/:routeId (сделано)
GET /saved (сделано)


8. Reviews

Отзывы и рейтинг маршрутов.

POST /routes/:id/review (не сделано)
GET /routes/:id/reviews (не сделано)
PATCH /reviews/:id (не сделано)
DELETE /reviews/:id (не сделано)


9. Что еще не сделано (общий список)

GET /auth/profile
GET /users/:id
PATCH /users/:id
GET /users/:id/routes
GET /routes?category=
GET /routes?author=
GET /routes?public=true
POST /routes/:id/review
GET /routes/:id/reviews
PATCH /reviews/:id
DELETE /reviews/:id
